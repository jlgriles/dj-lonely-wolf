# DJ Lonely Wolf

One-page site for DJ Lonely Wolf, a Richmond, VA club DJ. Static HTML/CSS/JS, no build step. Repo: `jlgriles/dj-lonely-wolf`.

## Sections

Single page (`index.html`), anchor-linked, built on the **Dimension** template by HTML5 UP:

- **Intro** (`#intro`) — Bio intro, DJ controller image.
- **Work** (`#work`) — Short description of live sets, DJ set image.
- **About** (`#about`) — Background: started on drums/guitar/bass/piano, moved into a home recording studio, then beat mixing.
- **Contact** (`#contact`) — Links to Spotify, SoundCloud, and Instagram. No contact form is live (markup exists in `index.html` but is commented out).

## Stack

- Static HTML/CSS/JS — no framework, no build tooling checked into the repo (no `package.json`/bundler).
- Template: **Dimension** by HTML5 UP (per `README.txt`), using Font Awesome for icons.
- `assets/sass/` contains the Sass source; `assets/css/main.css` is the compiled stylesheet actually loaded by `index.html`.

---
*Generated from a direct read of the repository contents. Update this file as pages, assets, or hosting configuration change — it isn't generated automatically.*
